<odoo noupdate="1">
    <record id="ir_cron_investment_to_so" model="ir.cron">
        <field name="name">Investment: Add to Sale Order</field>
        <field name="model_id" ref="model_investments_investment"/>
        <field name="state">code</field>
        <field name="code">model._cron_add_investments_to_sale_order()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now().replace(hour=2, minute=0, second=0)).strftime('%Y-%m-%d %H:%M:%S')"/>
    </record>
</odoo>
